<configure
    xmlns="http://namespaces.zope.org/zope"
    i18n_domain="knowledge.curator"
    >

  <!-- Graph Storage Adapter -->
  <adapter
      factory=".storage.GraphStorage"
      provides=".interfaces.IGraphStorage"
      for="*"
      />

  <!-- Include browser views -->
  <include package="knowledge.curator.browser" />

</configure>
